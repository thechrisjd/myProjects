import { industries, jobTypes, locations } from '../components/Profile/config'
import find from 'lodash/find'

export default {
  // methods: {
  //   getIndustryName(id) {
  //     if (!)
  //     return find(industries, {id}).name
  //   },
  //   getJobTypeName(id) {
  //     return find(jobTypes, {id}).label
  //   },
  //   getLocationNames(id) {
  //     return find(locations, {id}).label
  //   }
  // },
}